import AppOptions from "../appOptions";

export default class extends AppOptions {
    end?: any;
    limit?: number;
    revision?: number;
    paths?: boolean;
    start?: any;
    targets?: string[];
}